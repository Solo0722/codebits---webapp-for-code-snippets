"use client";
import React from "react";
import { styled } from "styled-components";

const Settings = () => {
  return <HomeWrapper>Settings</HomeWrapper>;
};

const HomeWrapper = styled.section`
  height: 100vh;
`;

export default Settings;
